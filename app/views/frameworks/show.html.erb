<h1><%= @framework.name %></h1>
<% if @framework.resources.any? %>
  <h4 align="center">Resouces' containing <%= @framework.name %></h4>
  <% @framework.resources.each do |resource| %>
  <h4><%= resource.url %></h4>
  <%= link_to 'Show', resource %>
  <%= link_to 'Edit', edit_resource_path(resource) %>
  <% end %>
<% else %>
  <h4 align="center">No resources yet!</h4>
<% end %>